spring:
  application:
    name: GraduateTracking
  datasource:
    url: jdbc:mysql://localhost:3306/graduate_tracking_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: pass123
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
server:
  port: 8080

